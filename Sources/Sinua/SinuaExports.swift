// The voice types the views take (`SinuaView(voice:voiceOverrides:)`), from SinuaVoiceTypes,
// which has no audio I/O. `import Sinua` alone is enough to pass them; the microphone and
// the vendor sources stay in the separate SinuaVoice product.
@_exported import SinuaVoiceTypes
